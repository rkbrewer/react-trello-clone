import Header from '@/components/Header';
import Layout from '@/layouts/default';

export default () => {
  return (
    <Layout>
      <p>
        This is the about pg.
      </p>
    </Layout>
  )
};
